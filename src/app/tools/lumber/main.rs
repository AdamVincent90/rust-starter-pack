// Lumber will be an eventual tool that will create store and core functions with a simple command.

fn main() {
    println!("This will be a tool to generate template files, for stores, cores, and handlers.")
}
